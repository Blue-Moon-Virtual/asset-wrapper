schema_version = "1.0.0"

id = "asset_master_collectionize"
version = "0.4.0"
name = "Asset Master — Collectionize"
tagline = "Turn selections or collections into linked assets in one click"
maintainer = "Blue Moon Virtual <info@bm-3d.de>"
type = "add-on"

website = "https://github.com/bluemoonvirtual/asset-master-collectionize"

tags = ["Object", "Pipeline", "Import-Export"]

blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Writes asset .blend files into the project's asset library folder"

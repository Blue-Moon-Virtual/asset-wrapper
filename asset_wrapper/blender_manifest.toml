schema_version = "1.0.0"

id = "asset_wrapper"
version = "0.5.2"
name = "Asset Wrapper"
tagline = "Wrap any selection or collection into a linked asset in one click"
maintainer = "Blue Moon Virtual <info@bm-3d.de>"
type = "add-on"

website = "https://github.com/Blue-Moon-Virtual/asset-wrapper"

tags = ["Object", "Pipeline", "Import-Export"]

blender_version_min = "4.2.0"

license = ["SPDX:GPL-3.0-or-later"]

[permissions]
files = "Writes asset .blend files into the project's asset library folder"
network = "Checks GitHub for add-on updates when you ask it to"
